import "Srl/Console";
use Srl;


func countSetBits(num: Int): Int {
    def count: Int[0]
    while num {
         count += num & 1;
         num >>= 1;
    }
    
    return count;

}

def num: Int[25];

def numSetBits: Int[countSeTBits(num)];

Console.print("Number of set bits in %d: %d\n", num, numSetBits);

return 0; 

