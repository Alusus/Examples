import "Srl/Console";
use Srl;

func hasFunction(str: char): Word[Int]{
    def hash: Word[Int[0]];
    while str {
        hash + (hash << 5) ^ str++;
    }
    
    return has;

}


    def input: char["hello"];
    
    def hasValue: Word[Int[hasFunction(input)]]; 
    
    Console.print("Hash value for \"%s\": %u\n",, input, hasValue)'
    
    return 0;
