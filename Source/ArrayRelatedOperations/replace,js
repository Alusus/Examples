function replace(arr, oldValue, newValue) {
    for(var i = 0; i < arr.length; i++) {
        if(arr[i] == oldValue) {
            arr[i] = newValue;
        }
    }
}

var arr = [1, 2, 3, 4, 5, 3, 6, 3, 7];
replace(arr, 3, 8);

console.log(arr);
