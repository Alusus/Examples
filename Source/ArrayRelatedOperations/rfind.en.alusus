import "Srl/Array";


use Srl;


func rfind(a: ref[Array[Int]], val: Int): Int {
    def i: Int;
    for i = a.getLength() - 1 , i != 0 , i-- {
        if a(i) == val {
            return i;
        }
    }
    return -1;
}
