import "Srl/Array";


use Srl;


func replace(a: ref[Array[Int]], oldValue: Int, newValue: Int) {
    def i: Int;
    for i = 0 , i < a.getLength() , i++ {
        if a(i) == oldValue {
            a(i) = newValue;
        }
    }
}
