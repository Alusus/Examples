import "Srl/Array";


use Srl;


func slice(a: ref[Array[Int]], start: Int, end: Int, result: ref[Array[Int]]) {
    def i: Int;
    for i = start , i <= end , i++ {
        result.add(a(i));
    }
}
