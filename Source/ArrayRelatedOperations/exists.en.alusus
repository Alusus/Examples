import "Srl/Array";


use Srl;


func exists(a: ref[Array[Int]], val: Int): Bool {
    def i: Int;
    for i = 0, i < a.getLength(), i++ {
        if a(i) == val {
            return true;
        }
    }
    return false;
}
