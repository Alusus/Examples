import "Srl/String";

use Srl;


function invertString(s) {
    def size: Int = s.getLength();
    def result: String = "";

    def i: Int;
    for i = size - 1 , i >= 0 , i-- {
        result += s(i);
    }

    return result;
}
