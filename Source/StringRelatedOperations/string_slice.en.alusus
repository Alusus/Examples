import "Srl/String";

use Srl;

func slice(s: ref[String], start: Int, end: Int): String {
    def result: String = "";

    def i: Int;
    for i = start , i <= end , i++ {
        result.append(s(i));
    }

    return result;
}
